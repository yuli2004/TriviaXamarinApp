<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TriviaXamarinApp.Views.ShowQuestion">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Question:"></Label>
            <Label Text="{Binding QText}" Grid.Column="0" Grid.Row="0" />
            <Label Text="Correct Answer:"/>
            <Label Text="{Binding QAnswer}" Grid.Column="1" Grid.Row="0"/>
            <Label Text="Incorrecct Answers:"/>
            <Label Text="{Binding QNotAnswers[0]}"  />
            <Label Text="{Binding QNotAnswers[1]}" />
            <Label Text="{Binding QNotAnswers[2]}" />


        </StackLayout>
    </ContentPage.Content>
</ContentPage>